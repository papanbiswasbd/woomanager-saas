'use client';

// Background 15s interval polling removed per user request.
// Instant updates happen automatically whenever orders/products are created or updated,
// or via Webhooks / manual sync button.

export function AutoSync() {
  return null;
}
